<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Login" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_green_def_light</item>
        <item name="colorOnPrimary">@color/on_primary_green_dark</item>
        <item name="colorPrimaryContainer">@color/primary_container_green_half</item>
        <item name="colorOnPrimaryContainer">@color/on_primary_container_green_light</item>
        <item name="android:windowFullscreen">true</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

</resources>